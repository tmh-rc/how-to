# How to create custom git repo

Initialize git
```
git init --bare
```

Clone git
```
git clone ssh://username@publicIP/PROJECT_PATH
```

Remote add
```
git remote add staging ssh://root@10.10.10.10/PROJECT_PATH
```
